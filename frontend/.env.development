# Dev-server defaults — used by `npm run dev`. Override personal values via
# `.env.development.local`.
VITE_API_BASE_URL=http://localhost:8086
VITE_KEYCLOAK_AUTHORITY=http://localhost:9085/realms/football-realm
